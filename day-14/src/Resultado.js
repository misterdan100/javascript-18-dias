function Resultado(props) {

    // seleccionar un numero aleatorio de la lista como el numero elegido
    // leer que numero el presionado y comprobar si es el elegido
    // mostrar mensaje de ganador o perdedor
    // si es perdedor cambiar estilos del numero ideal
    


    return (
        <div className="h-20">
            <p>{ props.mensaje }</p>
        </div>

    )
}

export default Resultado;